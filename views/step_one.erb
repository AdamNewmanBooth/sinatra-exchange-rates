<h1> Convert <%= @the_symbol %></h1>

<ul>

  <% @currencies.keys.each do |symbol| %>
    <li>
      <a href = "<%=@the_symbol%>/<%= symbol %>" /a>
       Convert 1 <%= @the_symbol %> to <%= symbol %>...
     </li>
  <% end %>

</ul>

<p>Go <a href="/">back</a></p>
